<UserControl x:Class="CncController.Views.Components.JogConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">

    <StackPanel Margin="5">
        <UniformGrid Columns="4" Margin="0,0,0,15">
            <RadioButton Content="Cont." GroupName="JogMode" Style="{StaticResource NavBtnStyle}" IsChecked="True"
                         Command="{Binding SetJogModeCommand}" CommandParameter="0"/>

            <RadioButton Content="10.0" GroupName="JogMode" Style="{StaticResource NavBtnStyle}"
                         Command="{Binding SetJogModeCommand}" CommandParameter="10.0"/>

            <RadioButton Content="1.0" GroupName="JogMode" Style="{StaticResource NavBtnStyle}"
                         Command="{Binding SetJogModeCommand}" CommandParameter="1.0"/>

            <RadioButton Content="0.1" GroupName="JogMode" Style="{StaticResource NavBtnStyle}"
                         Command="{Binding SetJogModeCommand}" CommandParameter="0.1"/>
        </UniformGrid>

        <DockPanel Margin="0,5">
            <TextBlock Text="Feedrate" Foreground="#DDD" VerticalAlignment="Center" FontSize="12"/>
            <TextBox Text="{Binding JogFeedrate}" Width="80" HorizontalAlignment="Right" Background="White" Padding="3" FontWeight="Bold" BorderThickness="0"/>
        </DockPanel>

        <DockPanel Margin="0,5">
            <TextBlock Text="Spindle" Foreground="#DDD" VerticalAlignment="Center" FontSize="12"/>
            <TextBox Text="300" Width="80" HorizontalAlignment="Right" Background="White" Padding="3" FontWeight="Bold" BorderThickness="0"/>
        </DockPanel>

        <UniformGrid Columns="3" Margin="0,20,0,0">
            <Button Content="REV" Height="40" FontSize="12" Margin="2" Background="#444" Foreground="White"/>
            <Button Content="STOP" Height="40" FontSize="12" Margin="2" Background="#D80000" Foreground="White"/>
            <Button Content="FWD" Height="40" FontSize="12" Margin="2" Background="#444" Foreground="White"/>
        </UniformGrid>
    </StackPanel>
</UserControl>