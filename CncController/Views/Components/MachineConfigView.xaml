<UserControl x:Class="CncController.Views.Components.MachineConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:CncController.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="250"
             d:DataContext="{d:DesignInstance Type=vm:MachineConfigViewModel}"
             FontFamily="{DynamicResource FontFamily.Default}">

    <UserControl.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSize.Medium}"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource Brush.Panel.Secondary}" CornerRadius="5" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" Padding="15">
        <StackPanel>

            <TextBlock Text="{DynamicResource Str.Setting.MachineAxisConfig}" 
                       Foreground="{DynamicResource Brush.Accent}"
                       FontSize="{DynamicResource FontSize.Large}"
                       FontWeight="Bold" 
                       Margin="0,0,0,15"/>

            <!-- [2026-02-24] 機台類型下拉選單 -->
            <TextBlock Text="{DynamicResource Str.Setting.MachineType}"
                       Foreground="{DynamicResource Brush.Text.Muted}"
                       FontSize="{DynamicResource FontSize.Body}"
                       Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding MachineTypeOptions}"
                      SelectedItem="{Binding SelectedMachineTypeItem}"
                      DisplayMemberPath="DisplayName"
                      Height="30" FontSize="{DynamicResource FontSize.Medium}" Margin="0,0,0,12"/>

            <Rectangle Height="1" Fill="{DynamicResource Brush.Separator}" Margin="0,0,0,10"/>

            <TextBlock Text="{DynamicResource Str.Setting.EnabledAxes}"
                       Foreground="{DynamicResource Brush.Text.Muted}"
                       FontSize="{DynamicResource FontSize.Body}"
                       Margin="0,0,0,10"
                       TextWrapping="Wrap"/>

            <StackPanel Margin="5,0,0,0">
                <CheckBox Content="{DynamicResource Str.Setting.AxisXBasic}" IsChecked="{Binding EnableX}"/>
                <CheckBox Content="{DynamicResource Str.Setting.AxisYBasic}" IsChecked="{Binding EnableY}"/>
                <CheckBox Content="{DynamicResource Str.Setting.AxisZBasic}" IsChecked="{Binding EnableZ}"/>
            </StackPanel>

            <Rectangle Height="1" Fill="{DynamicResource Brush.Separator}" Margin="0,15,0,10"/>

            <StackPanel Margin="5,0,0,0">
                <CheckBox Content="{DynamicResource Str.Setting.AxisARotary}" IsChecked="{Binding EnableA}"/>
                <CheckBox Content="{DynamicResource Str.Setting.AxisBRotary}" IsChecked="{Binding EnableB}"/>
                <CheckBox Content="{DynamicResource Str.Setting.AxisCRotary}" IsChecked="{Binding EnableC}"/>
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>