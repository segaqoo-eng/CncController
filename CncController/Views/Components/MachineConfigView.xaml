<UserControl x:Class="CncController.Views.Components.MachineConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CncController.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="250"
             d:DataContext="{d:DesignInstance Type=vm:MachineConfigViewModel}">

    <UserControl.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#252526" CornerRadius="5" BorderBrush="#3E3E42" BorderThickness="1" Padding="15">
        <StackPanel>

            <TextBlock Text="機台軸數設定" 
                       Foreground="#007ACC" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Margin="0,0,0,15"/>

            <TextBlock Text="請勾選機台實際啟用的軸：" 
                       Foreground="#AAAAAA" 
                       FontSize="12" 
                       Margin="0,0,0,10"
                       TextWrapping="Wrap"/>

            <Rectangle Height="1" Fill="#444" Margin="0,0,0,10"/>

            <StackPanel Margin="5,0,0,0">
                <CheckBox Content="X Axis (基本軸)" IsChecked="{Binding EnableX}"/>
                <CheckBox Content="Y Axis (基本軸)" IsChecked="{Binding EnableY}"/>
                <CheckBox Content="Z Axis (基本軸)" IsChecked="{Binding EnableZ}"/>
            </StackPanel>

            <Rectangle Height="1" Fill="#444" Margin="0,15,0,10"/>

            <StackPanel Margin="5,0,0,0">
                <CheckBox Content="A Axis (第4軸/旋轉)" IsChecked="{Binding EnableA}"/>
                <CheckBox Content="B Axis (第5軸/旋轉)" IsChecked="{Binding EnableB}"/>
                <CheckBox Content="C Axis (第6軸/旋轉)" IsChecked="{Binding EnableC}"/>
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>