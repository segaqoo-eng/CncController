<Window x:Class="CncController.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400"
        WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#252526" BorderBrush="#007ACC" BorderThickness="1">

    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="40">
            <TextBlock Text="SYSTEM LOGIN" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Password:" Foreground="#AAA" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtPassword" 
                         Height="40" FontSize="20"
                         Background="#333" Foreground="White"
                         BorderThickness="0" Padding="5"
                         HorizontalContentAlignment="Center"/>

            <Button Content="LOGIN" Click="BtnLogin_Click"
                    Height="45" Margin="0,30,0,0"
                    Background="#007ACC" Foreground="White" 
                    FontWeight="Bold" FontSize="16" BorderThickness="0" Cursor="Hand"/>

            <Button Content="Cancel" IsCancel="True"
                    Height="30" Margin="0,10,0,0"
                    Background="Transparent" Foreground="#888" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>