# 專案重新深掃 Prompt（智能維護版）

## 任務說明

專案大致完成，請重新深掃整個專案，**智能更新** `CLAUDE.md`。
所謂智能更新，是指你掃完後**自行判斷每個章節是否需要異動**，
而不是全部重寫，也不是全部保留。

---

## 掃描步驟

### 步驟一：掃描所有關鍵檔案（無層數限制）

```bash
# 掃描所有關鍵原始碼
find . -type f \( -name "*.cs" -o -name "*.xaml" -o -name "*.csproj" \
  -o -name "*.json" -o -name "*.xml" -o -name "*.md" -o -name "*.py" \) \
  -not -path "*/bin/*" \
  -not -path "*/obj/*" \
  -not -path "*/.git/*" \
  -not -path "*/node_modules/*" \
  | sort

# 掃描目錄結構
find . -type d \
  -not -path "*/bin/*" \
  -not -path "*/obj/*" \
  -not -path "*/.git/*" \
  | sort
```

### 步驟二：對照現有 CLAUDE.md，逐章節判斷

掃描完成後，請對照現有 `CLAUDE.md`，對**每一個章節**做出以下判斷：

```
✅ 內容與實際相符 → 保留，不動
📝 內容部分過時或有新增 → 局部修改（補充/刪除/修正）
🔄 內容與實際差異較大 → 整節重寫
```

#### 各章節的判斷重點

| 章節 | 判斷依據 |
|------|---------|
| `## 1. Project Overview` | 技術棧版本是否變動 |
| `## 2. Directory Structure` | 有無新增/刪除/移動的檔案或資料夾 |
| `## 3. Key Classes & Locations` | 行數是否變化、有無新增或移除的 class |
| `## 4. Architecture & Data Flow` | 通訊方式/模組結構是否有異動 |
| `## 5. 後端 API 端點` | server.py route 有無新增或移除 |
| `## 6. High-Risk Areas` | 有無新的高風險區域 |
| `## 7. 程式碼修改規範` | 有無新的規範需要補充 |
| `## 9. 程式碼放置規則` | 有無新的 Model 領域、新的 partial 檔、新的規則 |
| `## 10. Common Modification Patterns` | 有無新的常見修改場景需要補充 |
| `## 13. 功能完成狀態` | 哪些功能從 ❌ 變成 ✅，哪些新增了 ⚠️ |

---

### 步驟三：執行更新

- 每次修改處加上日期註解：`<!-- [YYYY-MM-DD] 說明異動原因 -->`
- **局部修改**時，只改動有變化的條目，其餘保留
- **§9 和 §10** 特別注意：
  - 新增了新 Model 檔案 → 在 §9.1 表格**補一行**
  - 新增了新 MainViewModel partial → 在 §9.2 表格**補一行**
  - 出現了新的常見修改場景 → 在 §10 **補一個 Pattern**
  - 某個檔案/規則已不存在 → **刪除對應條目**

---

### 步驟四：更新 memory.md

```markdown
## 最後掃描時間
[YYYY-MM-DD]

## 本次掃描異動摘要
（列出哪些章節改了什麼，哪些章節保留未動）

## 已知技術債 / 待優化

## 下次修改注意事項
```

---

### 步驟五：自我驗證

完成後請回答：

1. 哪些章節做了修改？修改了什麼？
2. 哪些章節保留未動？原因？
3. `§9` 和 `§10` 有無補充或刪除任何條目？
4. 有沒有發現文件記載與實際程式碼不一致的地方？
5. 有重構建議
---

## 核心原則

> 你是有判斷力的，掃完整個專案後你最清楚哪裡對、哪裡舊、哪裡漏。
> 不要盲目保留，也不要無腦重寫。
> 以**實際程式碼為準**，文件跟著現實走。
> 目標：讓下一個 AI session 不需要重新掃描，
  直接讀文件就能精準定位要修改的位置。
